package edu.buffalo.www.cse.odinlab.PocketBench;

import java.util.LinkedHashMap;
import java.util.List;
import edu.buffalo.www.cse.odinlab.PocketBench.Sessions.DataRow;
//import ca.pfv.spmf.algorithms.sequenceprediction.ipredict.database.Sequence;
//import ca.pfv.spmf.algorithms.sequenceprediction.ipredict.predictor.CPT.CPTPlus.*;
//import ca.pfv.spmf.algorithms.associationrules.TopKRules_and_TNR.*;

public class Recommender {
	/*
	
	CPTPlusPredictor predictor = new CPTPlusPredictor();
	
	
	public Recommender(List<Session> trainingSessions, List<Session> testSessions) {
		
		List<Sequence> trainingSequences = createTrainingSessions(trainingSessions);
		predictor.Train(trainingSequences);
		
		predictor.Predict(target);
	}
	
	
	public Recommender(List<Session> trainingSessions, List<Session> testSessions) {
		
		AlgoTopKRules topK = new AlgoTopKRules();
		
		Database database = new Database();
		
		database.addTransaction(itemsString);
		
		int k = 10;
		double minConfidence = 0.3;
		
		topK.runAlgorithm(k, minConfidence, database);
		
		
	}
	
	*/
	
}
